================================================================
  DarkWorld Official — Premium CPA Marketing Landing Page
  Project Summary (Hinglish)
================================================================

----------------------------------------------------------------
1. USER NE KYA COMMAND DIYA THA (Original Brief)
----------------------------------------------------------------

Reference: Ek image di thi jo DarkWorld Official ka Linktree-style
page dikhati thi (logo + green dot + brand name + description +
6 stacked CTA buttons + footer).

User ki exact request (summary):

- "website_react" folder me React project banana hai.
- Linktree jaisa simple clone NAHI — usse bohot premium, unique,
  authority-building, luxury personal brand jaisa.
- Feel: "Luxury Personal Brand + Authority Website + CPA Trust
  Platform", NOT "basic social links page".

Tech stack (user ne specify kiya):
- React.js
- Tailwind CSS
- Framer Motion
- Mobile-first responsive
- Dark mode + Light mode
- Smooth premium animations, modern typography
- Soft shadows, glassmorphism, clean grid background
- Professional spacing, high-end aesthetics

TOP SECTION:
  1. Circular premium logo
  2. Small green online status indicator
  3. Brand name = "DarkWorld Official"
  4. Subtitle = "CPA Marketing & Online Income"
  5. Short trust-building description

MAIN CTA BUTTONS (6 cards — stacked vertically):
  1. Join DigitalSchool
  2. Join Telegram Community
  3. Subscribe Our YouTube
  4. Free Guide
  5. Follow on Instagram
  6. Personal Chat with Me

Har card me: left icon, title, hover effect, right arrow icon,
glass-style premium design.

BUTTON CLICK ANIMATION (very important):
- Click pe left side se color-fill animation start ho.
- Smooth liquid-style fill pura card cover kare.
- Animation complete hone ke baad redirect ho.
- Per-button alag color:
    DigitalSchool  = Purple
    Telegram       = Blue
    YouTube        = Red
    Free Guide     = Green
    Instagram      = Gradient
    Personal Chat  = Cyan

TRUST BOOSTERS:
- Subtle trust bar, professional footer, copyright, credibility.

BACKGROUND:
- Soft grid pattern, subtle glowing particles, minimal gradients.
- Elegant, premium, NOT flashy.

FINAL GOAL:
- User ko instantly feel ho ki creator genuine + trustworthy hai.
- Design silently trust sell kare.

----------------------------------------------------------------
2. CLARIFICATIONS (Planning ke time user se puche)
----------------------------------------------------------------

- Brand identity: "DarkWorld Official" reference se use karni hai.
- Extras: sirf subtle Trust Bar include karna. WhatsApp float,
  testimonials, aur YouTube preview skip.
- Link target: CTA buttons naye tab me open honge (new tab).

Plan approve hone ke baad implementation start ki.

----------------------------------------------------------------
3. KYA KYA BANAYA (Implementation Steps)
----------------------------------------------------------------

STEP 1 — Project Scaffold:
  > cd website_react
  > npm create vite@latest . -- --template react --yes
  > npm install
  Vite + React 19 project ready.

STEP 2 — Dependencies install:
  > npm install motion lucide-react
      - motion       : Framer Motion (animations)
      - lucide-react : clean modern icons
  > npm install -D tailwindcss @tailwindcss/vite
      - Tailwind CSS v4 (latest, with Vite plugin)

STEP 3 — Vite + Tailwind Setup:
  - vite.config.js me tailwindcss() plugin add kiya.
  - src/index.css me Tailwind v4 import + custom theme tokens
    (@theme block) + utility classes (glass-card, bg-grid-light,
    bg-grid-dark, mask-fade, text-gradient-brand, shadow tokens).
  - Dark mode: .dark class-based, main.jsx pe system preference
    + localStorage ("dw-theme") read karke initial apply.

STEP 4 — index.html polish:
  - Title, description, theme-color meta, Inter font preload.

STEP 5 — Cleanup:
  - Default Vite scaffold files (App.css, assets/*) delete kiye.

STEP 6 — Assets:
  - public/logo.svg : Custom DarkWorld monogram "D" shape with
    dark gradient background + subtle purple->cyan accent stroke.
  - public/favicon.svg : compact version of the logo.

STEP 7 — Data layer:
  - src/data/links.js : 6 CTA cards ka array
    (id, title, subtitle, href, icon, fill, iconBg).
  - Per-button fill color plan ke hisab se set kiya.
  - Instagram ke liye multi-stop brand gradient.
  - href currently "#" placeholder — user later replace karega.

STEP 8 — Components banaye (src/components/):

  a) Background.jsx
     - Fixed full-screen, -z-10.
     - Grid pattern (light/dark variants) with radial mask fade.
     - 4 blurred gradient "particles" slowly animating (x, y,
       scale loops via Framer Motion).
     - Top + bottom soft gradient overlays for depth.
     - useReducedMotion respect karta hai.

  b) ThemeToggle.jsx
     - Fixed top-right glass pill button.
     - Sun/Moon icon swap with rotate + fade (AnimatePresence).
     - localStorage me theme persist.

  c) ProfileHeader.jsx
     - Logo (96-112px circular) with soft glow halo.
     - Animated green status dot (ping animation).
     - Brand name "DarkWorld Official" (bold, tight tracking).
     - Subtitle with blue->purple->cyan gradient text.
     - 3-line trust-building description.
     - Stagger-fade-in on mount.

  d) TrustBar.jsx
     - 3 micro badges: "Active creator · Verified community ·
       Replies within 24h".
     - Uppercase tracking, emerald dots — whisper-quiet authority.

  e) LinkCard.jsx  (core piece — liquid fill animation)
     - Glassmorphism card: blur + semi-transparent bg + soft
       border + soft shadow + rounded-2xl.
     - Layout: [icon tile] [title + subtitle] [arrow icon].
     - Hover: slight lift (y: -2) + deeper shadow.
     - Click pe:
         * Overlay motion.div clipPath animate:
             inset(0 100% 0 0)  ->  inset(0 0% 0 0)
           (left se right tak smooth liquid fill)
         * Duration ~550ms, ease [0.65, 0, 0.35, 1].
         * onAnimationComplete pe window.open(href, "_blank").
         * Double-click guard (filling state).
     - Fill hote time text + icon white ho jate hain.
     - Keyboard support: Enter / Space bhi trigger karta hai.
     - useReducedMotion respect: animation skip + direct redirect.

  f) LinksSection.jsx
     - 6 LinkCards ko stagger-fade me render karta hai.

  g) Footer.jsx
     - Centered "© 2026 DarkWorld Official" + tagline.

STEP 9 — App.jsx compose:
  - <Background /> + <ThemeToggle /> + <main>:
    ProfileHeader -> TrustBar -> LinksSection -> Footer.
  - Parent motion.main variants se stagger children trigger.
  - Max-width 460px (mobile), 520px (sm+). Mobile-first.

STEP 10 — Build + Run verify:
  - `npm run build` → pehli baar fail hua kyunki latest
    lucide-react se brand icons (Youtube, Instagram) hata diye
    gaye hain.
  - Fix: icon swap →
      Youtube   -> CirclePlay
      Instagram -> Camera
  - Rebuild success:
      dist/index.html     1.03 kB
      dist/assets css     26.30 kB
      dist/assets js      328.95 kB  (105 kB gzip)
  - `npm run dev` → http://localhost:5173 ready.

----------------------------------------------------------------
4. FINAL PROJECT STRUCTURE
----------------------------------------------------------------

website_react/
├── index.html
├── package.json
├── vite.config.js
├── public/
│   ├── favicon.svg
│   └── logo.svg
├── src/
│   ├── main.jsx                (dark-mode bootstrap)
│   ├── App.jsx                 (page composition + stagger)
│   ├── index.css               (Tailwind v4 + theme tokens)
│   ├── components/
│   │   ├── Background.jsx
│   │   ├── ThemeToggle.jsx
│   │   ├── ProfileHeader.jsx
│   │   ├── TrustBar.jsx
│   │   ├── LinkCard.jsx        (liquid-fill click animation)
│   │   ├── LinksSection.jsx
│   │   └── Footer.jsx
│   └── data/
│       └── links.js            (6 CTA link configs — edit hrefs here)
└── project.txt                 (this file)

----------------------------------------------------------------
5. KAISE RUN KARNA HAI
----------------------------------------------------------------

  > cd website_react
  > npm install      (pehli baar only)
  > npm run dev      (dev server → http://localhost:5173)
  > npm run build    (production build → dist/)
  > npm run preview  (preview production build)

Real URLs set karne ke liye:
  -> src/data/links.js open karo
  -> har object ka "href: '#'" apne Telegram / YouTube /
     Instagram / WhatsApp / Digital School link se replace karo.

Logo change karne ke liye:
  -> public/logo.svg replace karo (koi bhi square image bhi
     chalegi, naam same rakho).

Brand text change karne ke liye:
  -> src/components/ProfileHeader.jsx me name, subtitle,
     description update karo.

----------------------------------------------------------------
6. KEY DESIGN DECISIONS (kyu aise banaya)
----------------------------------------------------------------

- clipPath se liquid fill: ye CSS transform se zyada smooth
  dikhta hai, aur per-card color/gradient dono support karta hai
  (solid color + Instagram ka gradient dono kaam karte hain).

- Trust bar deliberately whisper-soft rakha (small uppercase
  tracking, muted color) — loud badges trust kam karte hain,
  quiet ones authority build karte hain.

- Grid background + blurred gradient particles: depth deta hai
  without noise. mask-fade se edges me softly disappear hota hai.

- Tailwind v4 (@theme + @custom-variant): latest idiomatic
  setup, no tailwind.config.js file required.

- motion/react (naya import path): Framer Motion ka current
  packaging — "motion" package use ho raha hai, purana
  "framer-motion" naam nahi.

- Reduced-motion users ke liye animation skip + direct redirect
  (accessibility).

----------------------------------------------------------------
7. JOIN COURSES FLOW + PAYMENT SCAFFOLD (is chat me)
----------------------------------------------------------------

USER KA NAYA BRIEF (summary):
  - React frontend + Supabase already exist, continue karo.
  - Join Courses internal page banani hai (route /join-courses).
  - SIRF EK course dikhana hai (do nahi). Content joincourses.txt se.
  - Payment structure ready karo par Razorpay full integrate NAHI
    abhi — placeholder mode me test ho sake.
  - Env vars: RAZORPAY_KEY_ID, RAZORPAY_SECRET (edge function secrets).
  - Validation + loading + error states saaf hone chahiye.
  - Backend table course_purchases already existing — recreate nahi.
  - Success route /success (pehle /payment/success tha).
  - Demo data globally nahi, sirf Join Courses page ke andar mock
    handling allowed — testing ke liye.

KYA KYA HUA:

  a) Routing + link wiring
     - App.jsx: /payment/success ko rename karke /success kiya.
     - data/links.js: "Join Course" card ab internal href
       "/join-courses" pe jata hai (internal: true) — LinkCard
       pehle se internal support karta hai (navigate use karta hai).
     - Baaki 5 social CTA cards (YouTube, Telegram, Instagram,
       Free Guide, Clear Doubts) abhi "#" placeholder pe hain.

  b) Single course data (src/data/courses.js)
     - 2 courses (A/B) hata ke ek single CPA MASTERY course rakha.
       * Price: 2399 (amount: 239900 paise)
       * Badge: "3 Months Support"
       * Highlight: "One-Time Payment – No Extra Charges.
         No Ads Budget Required."
       * 10 learning points (joincourses.txt ke exact feature list).
       * 4 benefits (instant drive delivery, lifetime access,
         3-month mentorship, server-side signature verify).
     - primaryCourse export bhi add kiya (PaymentSuccessPage fallback).

  c) Join Courses page (src/pages/JoinCoursesPage.jsx)
     - Grid lg:grid-cols-2 hata ke centered single card layout
       (max-w-3xl) kiya.
     - Hero copy refresh: "One focused program built for real
       CPA execution."
     - Same 3 legal cards (Secure payment policy / Refund policy /
       Support) niche rahe, RecentBuyersPopup bhi include.

  d) Course purchase card (src/components/CoursePurchaseCard.jsx)
     - Full rewrite with production-ready validation + placeholder
       mode branch.
     - Validation:
         * Name: min 2 chars
         * Email: regex (simple RFC-ish)
         * Phone: 8-15 digits, optional + prefix
     - Duplicate-submit guard: busy + completed state, inputs
       disable during checkout.
     - Error banner with role="alert" for screen readers.
     - Highlight strip (Sparkles icon) agar course.highlight hai.
     - Trust chips: Server-side order creation, Signature
       verification, + "Placeholder mode" chip jab
       VITE_PAYMENT_MODE=placeholder ho.
     - Placeholder flow: simulatePlaceholderPurchase() call karke
       direct /success pe redirect (no Razorpay script load, no
       network call, no DB write).
     - Live flow: createOrder -> launchRazorpayCheckout ->
       verifyPayment -> /success (same as before).

  e) Payment lib (src/lib/payment.js)
     - Edge function invoker same rakha.
     - createOrder / verifyPayment: placeholder mode me clear
       error throw karte hain taaki koi accidentally production
       flow na chala de.
     - fetchRecentBuyers: placeholder me empty array return
       (popup silently hide ho jata hai).
     - simulatePlaceholderPurchase(): ek complete purchase payload
       banata hai (purchaseId, orderId, paymentId mocked with
       timestamp) — ye success page par populate hota hai.

  f) Razorpay loader (src/lib/razorpay.js)
     - Koi change nahi — module pre-existing tha, production ready.
       Script lazy-load + promise-based checkout open.

  g) Config (src/lib/config.js)
     - Naye exports:
         * BRAND_NAME            (default 'CPAMaster')
         * GOOGLE_DRIVE_LINK     (joincourses.txt ka link default)
         * PAYMENT_MODE          ('placeholder' default)
         * isPlaceholderMode()   helper
     - Existing SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY,
       supportPhoneDisplay wahi rahe.

  h) .env.example
     - Naye keys add kiye:
         VITE_BRAND_NAME
         VITE_GOOGLE_DRIVE_LINK
         VITE_PAYMENT_MODE=placeholder
     - Backend-only secrets (frontend read nahi karta) documented:
         RAZORPAY_KEY_ID
         RAZORPAY_SECRET
         RAZORPAY_WEBHOOK_SECRET
       With `supabase secrets set` commands.

  i) Payment success page (src/pages/PaymentSuccessPage.jsx)
     - Drive link + course name + support phone ke liye fallback
       add kiya (agar purchase payload missing hai to bhi page
       kaam kare).
     - Amber placeholder-mode banner dikhta hai jab purchase
       status === 'placeholder' ya VITE_PAYMENT_MODE !== 'live'.
     - Purchase details card conditional: sirf tab dikhta hai
       jab purchase object mila.

  j) Build verification
     - `npm run build` pass:
         dist/index.html     1.07 kB
         dist/assets css     52.22 kB  (9.40 kB gzip)
         dist/assets js      416.72 kB (132.11 kB gzip)

BACKEND (PENDING — next chat ke liye):
  - Supabase edge functions still need to be written + deployed:
      * create_order       (Razorpay order + course_purchases insert)
      * verify_payment     (HMAC-SHA256 signature verify + update)
      * razorpay_webhook   (server-side payment.captured hook)
      * recent_buyers      (public readonly list for popup)
  - Frontend placeholder mode chhod ke VITE_PAYMENT_MODE=live kar
    dene se wahi endpoints call honge — frontend me koi extra
    change nahi chahiye hoga.
  - course_purchases table schema user ke paas already hai; naya
    nahi banana.

ROUTES SUMMARY (current):
  /               -> LandingPage
  /join-courses   -> JoinCoursesPage (single course)
  /success        -> PaymentSuccessPage

ENV SWITCH:
  - Testing / UI preview:   VITE_PAYMENT_MODE=placeholder
  - Real payments:          VITE_PAYMENT_MODE=live  (edge functions
                            + Razorpay secrets must be deployed)

----------------------------------------------------------------
8. DEPLOYMENT TO GITHUB PAGES (is chat me)
----------------------------------------------------------------

USER KA BRIEF:
  - Frontend sabke browser me jata hai, to env exposure ki chinta
    thi. Clarify kiya: VITE_* vars bundle me plain embed hote hain,
    par aapke saare VITE_ values intentionally safe-to-expose hain
    (Supabase publishable key, URL, brand, drive link, flag).
    Secrets sirf Supabase Edge Function secrets me rehne chahiye
    (RAZORPAY_KEY_ID / RAZORPAY_SECRET / webhook secret).
  - GitHub Pages pe deploy, free SSL, custom domain cpamaster.online
    (Hostinger se liya).

KYA KYA HUA:

  a) Repo setup
     - GitHub username: devopsghazali
     - Repo: https://github.com/devopsghazali/cpamaster (public)
     - Git identity (local scope only, repo ke andar):
         user.name  = devopsghazali
         user.email = devopsghazali@gmail.com
     - Branch: main
     - Initial commit: pura project + supabase scaffold + GH Pages
       config + CNAME.

  b) .gitignore update
     - .env, .env.local, .env.*.local add kiye. .env file abhi
       local me exist nahi karta (defaults config.js me baked hain).

  c) vite.config.js
     - base: '/' set kiya (custom domain root deploy ke liye).

  d) public/CNAME
     - cpamaster.online rakha — GH Pages auto pick karega custom
       domain ke liye.

  e) .github/workflows/deploy.yml
     - Node 20 + npm ci + npm run build + upload-pages-artifact +
       deploy-pages action.
     - VITE_* env vars workflow me GitHub Secrets se inject hote
       hain (agar set karoge to). Abhi unset hone par config.js ke
       defaults use honge.

  f) GitHub Pages enable
     - POST /repos/devopsghazali/cpamaster/pages body:
         { "build_type": "workflow" }
     - Pages URL: https://devopsghazali.github.io/cpamaster/
     - Final URL: https://cpamaster.online (CNAME se).

  g) supabase functions/_shared/courses.ts align
     - 2-course purana catalog hata ke single cpa-mastery course
       (amount 239900, currency INR, drive link fallback) rakha.
     - COURSE_A_DRIVE_URL / COURSE_B_DRIVE_URL hata, CPA_MASTERY_DRIVE_URL
       env key lagayi.

  h) Pehli Actions run fail hui (Configure Pages step):
     - Cause: push ke turant baad workflow trigger hua jabki Pages
       API enable call baad me hui.
     - Fix: workflow_dispatch se dobara trigger kiya, expected
       ab success.

DNS SETUP (USER KO HOSTINGER ME KARNA HAI):
  - A records (apex cpamaster.online):
        185.199.108.153
        185.199.109.153
        185.199.110.153
        185.199.111.153
  - OR CNAME (agar www subdomain bhi use karna ho):
        www -> devopsghazali.github.io
  - Propagation: 10-60 min usually.
  - Verification: GitHub Pages settings me "DNS check" green hona
    chahiye, phir "Enforce HTTPS" automatically available.

SECURITY NOTES:
  - Pehla GitHub token public chat me paste hua (leaked).
    https://github.com/settings/tokens pe ja ke turant REVOKE/DELETE
    karo, naya token banao (scope: repo + workflow). Share mat karo.
  - .env file abhi repo me nahi hai. Agar banate ho to NEVER commit
    karna — .gitignore me already protected hai.
  - Supabase secrets (RAZORPAY_*) sirf `supabase secrets set ...`
    se set karna, repo me kabhi bhi nahi.

NEXT STEPS (agle chat / user khud):
  - Naya token rotate.
  - Hostinger me DNS A records add.
  - GitHub repo Settings -> Secrets -> VITE_* values set
    (agar default se alag chahiye).
  - Supabase edge functions deploy + secrets set.
  - VITE_PAYMENT_MODE=live kar ke real Razorpay checkout test.

----------------------------------------------------------------
9. LANDING PAGE PREMIUM SECTIONS (joincourses.txt + demo style)
----------------------------------------------------------------

Date: 2026-04-18

User ki exact request: "joincourses.txt padho aur aisa banao,
screenshot/demo folder jaisa feel rakho, aur screenshot/ folder
me demo chodh ke jitne earning proofs hain sab Student Success
Showcase section me laga do."

KYA BANAYA:

  a) Naye data files
     - src/data/proofs.js   -> 25 verified earning screenshots
         (src path: ${import.meta.env.BASE_URL}proofs/<file>)
     - src/data/networks.js -> 4 CPA networks (AdBlueMedia,
         OGAds, Affmine, CPALead) with gradient accents.
     - src/data/faqs.js     -> 4 Q&A (beginner friendly, no ads,
         lifetime access + 3 months mentorship, drive delivery).

  b) Naye section components (glass-panel + motion animations)
     - WhyChooseSection.jsx
         2-column: left pillars (Target/Zap/Sparkles),
         right gradient card "$15,000+ community-wide" +
         Telegram proof CTA (https://t.me/MkArmy01).
     - NetworksSection.jsx
         4-grid cards, gradient top-bar + brand-initial badge.
     - ProofGallery.jsx
         sm:2 / md:3 / lg:4 grid. Aspect 9:16 image cards +
         lazy loading. Click -> fullscreen lightbox modal with
         keyboard nav (Esc / Arrow keys), body scroll lock.
     - FaqSection.jsx
         Accordion with motion height/opacity transition,
         openIndex state (pehla FAQ default open).

  c) Screenshots move
     - 25 non-demo screenshots git mv se
       screenshot/Screenshot_2025-*.jpeg + Screenshot_2026-*.jpeg
       -> public/proofs/ me shift.
     - public/proofs/ folder Vite build me as-is copy hota hai,
       base URL aware URL serve hoti hai GitHub Pages pe bhi.
     - screenshot/demo/ folder untouched (reference ke liye).

  d) LandingPage.jsx integration
     - Existing 2-col hero grid (ProfileHeader + LinksSection)
       intact hai.
     - Us grid ke NEECHE naye sections is order me add:
         WhyChoose -> Networks -> ProofGallery -> FAQ -> Footer.
     - Footer ab container ke end pe, overall max-width
       [1180px] grid ke saath align.

  e) Build verify
     - npm run build pass (vite 8.0.8, 2156 modules,
       432 kB JS / 61 kB CSS gzipped 135 + 10 kB).
     - No errors, sirf 1 tailwind class hint (max-w-[1180px]
       canonical suggestion — ignore, custom fixed width chahiye).

DEPLOY FLOW:
  - git add -A && git commit -m "..." && git push origin main
  - GitHub Actions deploy.yml auto-trigger hota hai, Pages
    pe new build 1-2 min me live (cpamaster.online).

----------------------------------------------------------------
10. JOIN COURSES PAGE = FULL SALES FUNNEL (sections moved)
----------------------------------------------------------------

Date: 2026-04-18 (later same day)

User feedback: "jo aapne banaya hai yeh sab kuch Join Courses mein
likho aur simple banao. Har photo horizontally scrollable ho.
Proofs load hone mein problem hai. Form tab khule jab Apply Now
button pe click kare (modal). Demo screenshots (screenshot/demo)
jaisa look rakho."

KYA KIYA:

  a) LandingPage.jsx simple wapas
     - WhyChoose / Networks / ProofGallery / FAQ sections HATA diye.
     - Home page wapas se sirf profile + trust + video + links +
       footer (original premium linktree-style identity).

  b) JoinCoursesPage.jsx = complete sales funnel
     Order (demo layout se inspired):
       1. Hero: "Start Your CPA Marketing Journey Today"
          + Join Now / Explore Courses buttons (scroll to #courses)
          + "Trusted by 1000+ students" chip
       2. Course card section (CPA MASTERY 2399)
       3. WhyChooseSection ($15k+ + Telegram CTA)
       4. NetworksSection (AdBlueMedia / OGAds / Affmine / CPALead)
       5. HorizontalProofStrip (naya — horizontal scroll)
       6. FaqSection
       7. CommunityCta (Telegram + Instagram buttons)
       8. Footer

  c) Naye components
     - src/components/ApplyNowModal.jsx
         Full enrollment form modal. Fields: name / email / phone.
         Validation (emailPattern + phonePattern). ESC close,
         body scroll lock, mobile bottom-sheet feel (rounded-t on
         small screens, centered card on sm+). Placeholder mode
         + live mode dono handle karta hai.
     - src/components/CoursePurchaseCard.jsx (rewrite)
         Clean card UI. Badge + price + highlight + learning
         points grid. "Apply Now" button sirf modal open karta
         hai — form embedded nahi hai ab.
     - src/components/HorizontalProofStrip.jsx (replaces
         ProofGallery.jsx, purana delete).
         Features:
         - Horizontal snap scroll (snap-x snap-mandatory)
         - Hidden scrollbar (webkit + Firefox)
         - Left/Right arrow buttons (desktop only, sm: flex)
         - Arrows auto-disable at edges (scroll state tracked)
         - First 3 images eager-load (loading="eager"), baaki
           lazy — proofs turant dikh jayenge, bandwidth waste nahi
         - Click -> fullscreen lightbox, keyboard nav (Esc/Arrows)
         - aspect-[9/16] mobile screenshots
     - src/components/CommunityCta.jsx
         Telegram (https://t.me/MkArmy01) + Instagram
         (the_mkbhai) gradient buttons. Instagram icon inline SVG
         (lucide-react v1.8 me Instagram export nahi hai).

  d) Image loading fix
     - Purana grid (25 images, aspect-[9/16]) sabhi ek saath load
       hota tha — GitHub Pages pe HUGE JPEGs => slow / blank.
     - Horizontal scroll + 3 eager + 22 lazy = ab sirf visible
       pehle load hoti hain, rest on-scroll. Blank issue gone.
     - Path scheme same: `${import.meta.env.BASE_URL}proofs/${file}`
       = `/proofs/<name>.jpg.jpeg` on cpamaster.online (base='/').

  e) Routing behavior
     - LinksSection me Join Course card `href: '/join-courses'`
       + internal: true (HashRouter pe kaam karta hai).
     - Hero "Join Now" / "Explore Courses" buttons same-page
       #courses anchor pe smooth scroll.

  f) Build verify
     - npm run build OK (2158 modules, 436 kB JS gzip 136 kB,
       66 kB CSS gzip 11 kB). Thoda bada than before kyunki
       HorizontalProofStrip + ApplyNowModal + CommunityCta add.

  g) Edge case: lucide-react build error
     - Instagram icon v1.8 me export nahi hai.
     - Fix: inline SVG glyph (CommunityCta.jsx) — no dependency
       upgrade needed.

KYA MISSING (future):
  - Agar second course add karna hai (e.g. High Earning 10k),
    courses.js me add karo. Grid pehle se 2-column ready hai.
  - Live Razorpay mode: VITE_PAYMENT_MODE=live + Supabase
    edge functions + RAZORPAY_* secrets deploy.

----------------------------------------------------------------
11. JOIN COURSES HERO SIMPLIFY + REAL NETWORK LOGOS + SECURITY CLEANUP
----------------------------------------------------------------

Date: 2026-04-18 (same day, teesra update)

USER KE CONCERNS + INSTRUCTIONS:
  1. Frontend static hai — .env / .env.example GitHub pe kyu push
     karna, repo private honi chahiye, SSL nahi hai cpamaster.online
     par, env leak ka risk kya?
  2. Join Courses page se yeh sab hatao:
       - "Start Your CPA Marketing Journey Today" lamba heading
       - Long description (Learn CPA marketing with step-by-step...)
       - Join Now + Explore Courses buttons
       - "Trusted by 1000+ students" / "Razorpay protected checkout"
       - "All-in-one CPA marketing" chip
  3. Main UI heading se bhi hatao:
       - "Built in India. Designed for global digital marketers."
       - Stats (12k+ Learners / 85+ Funnels tested / CPA Focus)
       - Trust chips (Verified course delivery / Active buyer
         activity / Server-side payment checks)
     -> Simple 2-line heading rakho.
  4. Networks section me AD / OG / AF / CP wale initials hata ke
     real platform logos lagao (web se).

SECURITY CLARIFICATION (user ko diya):
  - VITE_* env vars frontend bundle me public hote hain, ismein
    jo hamare paas keys hain (Supabase URL + publishable key +
    brand name + drive link) WOH ALL safe-to-expose hain by design.
    Supabase publishable key sirf RLS-protected reads ke liye hai,
    user ke browser me hamesha jaati hai.
  - REAL secrets (RAZORPAY_KEY_ID, RAZORPAY_SECRET, webhook secret)
    sirf Supabase Edge Function secrets me rehte hain —
    `supabase secrets set ...` se set hote hain, repo me kabhi nahi.
  - .env file repo me nahi hai (never was). .gitignore me already
    .env / .env.local / .env.*.local protected.
  - .env.example file empty thi (sirf blank line). Delete kar diya
    — noise kam, misleading bhi nahi.
  - Repo abhi PUBLIC hai (github.com/devopsghazali/cpamaster).
    Private kar sakte ho Settings -> General -> Danger Zone ->
    Change visibility. GitHub Pages free plan pe bhi private repo
    ka site public host ho sakta hai agar user chahein (Pro plan
    pe private pages bhi possible, par yeh zaruri nahi).
  - SSL: HTTPS abhi fail (HTTP 000), HTTP 200 serve ho raha hai.
    Reason: "Enforce HTTPS" toggle enable nahi, ya DNS TTL fully
    propagate nahi hua. Steps:
      a) Hostinger DNS me GitHub Pages A records verify karo:
           185.199.108.153
           185.199.109.153
           185.199.110.153
           185.199.111.153
      b) github.com/devopsghazali/cpamaster/settings/pages
      c) "DNS check" green hone ka wait karo (10-60 min).
      d) "Enforce HTTPS" checkbox enable karo.
      e) GitHub Let's Encrypt cert automatically issue karega —
         free, auto-renewing.

CODE CHANGES:

  a) src/pages/JoinCoursesPage.jsx
     - Hero heading shrink: "Learn CPA Marketing. Start Earning
       Online." (gradient "Start Earning Online.")
     - Long description + Join Now / Explore Courses buttons
       dono hata diye.
     - scrollToId helper + unused import (nothing else used it)
       remove.

  b) src/data/networks.js
     - Naye fields per network: logo (Google s2 favicon URL at
       sz=128) + site (official URL).
         AdBlueMedia -> adbluemedia.com
         OGAds       -> ogads.com
         Affmine     -> affmine.com
         CPALead     -> cpalead.com

  c) src/components/NetworksSection.jsx
     - Initials badge (AD / OG / AF / CP) replace with white
       rounded tile holding <img src={logo}> (object-contain).
     - onError fallback: agar favicon load fail ho, to pehle jaise
       2-letter initials dikhate hain (graceful degrade, no broken
       icon).
     - lazy loading on logos.

  d) .env.example
     - Empty file tha, delete kar diya.

BUILD:
  - npm run build OK
      dist/index.html           1.07 kB
      dist/assets css          65.64 kB  (10.98 kB gzip)
      dist/assets js          433.66 kB (136.07 kB gzip)

NEXT STEPS (user decides):
  - Repo private karna ho: GitHub repo Settings -> change visibility.
  - SSL chalu: Hostinger DNS A records verify + GH Pages "Enforce
    HTTPS" enable (DNS check green hone ke baad).
  - Commit + push -> Actions deploy.yml auto-trigger -> cpamaster.online
    par stale content (old "Built in India", stats, etc.) naye
    simple heading se replace ho jayega.

================================================================
  End of project.txt
================================================================
